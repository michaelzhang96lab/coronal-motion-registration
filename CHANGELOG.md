# Changelog

## v0.1.0
- Initial release:
  - timelapse ingestion
  - global translation estimation (phase correlation)
  - feature tracking (template matching with local search)
  - reproducible outputs (CSV + plot + annotated GIF)
